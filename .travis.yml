language: PHP
dist: trusty
addons:
  sonarcloud:
    organization: "vansangnina-1"
    token:
      secure: ece6f555e46622c577441bd9eff0c4b309c6eb3a # encrypted value of your token

script:
  # the following command line builds the project, runs the tests with coverage and then execute the SonarCloud analysis
  - sonar-scanner
